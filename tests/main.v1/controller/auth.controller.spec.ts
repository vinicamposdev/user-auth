
describe('auth.controller.register()', () => {
  describe('Given valid params When the user is trying to register in the system', () => {
    test.todo('Then This API must accept the following FORM params in the POST method: email (required and unique)',
    // async () => {
    //   // arrange
    //   const user: User = {
    //     email: 'email',
    //     password: 'password',
    //     name: 'name',
    //     dob: new Date(),
    //     role: Role.Guest,
    //   }
    //   // act
    //   const registeredUser = await register(user);
    //   // assert
    //   expect(registeredUser.email).toBe(user.email);
    // }
    );
    // ensure password is encrypted
    test.todo('ensure password is encrypted');
  });

  describe('Given invalid params When the user is trying to register in the system', () => {
  });
});
